
  <span><h2>New booking for <%= @pet.name.capitalize %></h2></span>
  <!--   <div class="form-container"> -->
      <div class="container">
        <div class="row">
          <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">

            <%= simple_form_for [@pet, @booking] do |f| %>


            <%= f.input :booking_start, label: 'Booking Start', as: :string , :required => true, input_html: { class: 'datepicker'} %>

            <%= f.input :booking_end, label: 'Booking End', as: :string, input_html: { class: 'datepicker'}, :required => true %>

            <%= f.button :submit, "Book!", class: "btn btn-primary btn-block" %>
          </div>
      <% end %>
        </div>
      </div>
 <!--    </div> -->
  </div>


 <!--  <% if @booking.errors.any? %>
        <div class="errors-container">
          <ul>
            <% @booking.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %> -->


